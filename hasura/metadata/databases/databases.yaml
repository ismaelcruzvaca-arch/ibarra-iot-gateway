- name: default
  kind: postgres
  configuration:
    connection_info:
      database_url:
        from_env: NHOST_DATABASE_URL
      isolation_level: read-committed
      use_prepared_statements: true
  tables:
    - "!include default/tables/telemetry_telemetry_events.yaml"
