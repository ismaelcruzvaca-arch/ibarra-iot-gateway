{
  "name": "gema-edge-worker",
  "version": "1.0.0",
  "private": true,
  "description": "GEMA V3.0 Edge Worker — MQTT to SQLite",
  "main": "edge_worker.js",
  "scripts": {
    "start": "node edge_worker.js",
    "test": "node --test edge_worker.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "mqtt": "^5.10.0"
  },
  "devDependencies": {
    "sql.js": "^1.14.1"
  }
}
